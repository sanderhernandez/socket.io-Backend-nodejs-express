/// GIT

// Inicializa el git local en la PC:
git init

// configura el git:
git config --global user.name "sanderhernandez"
git config --global user.email "sanderhernandez@gmail.com"

// Hace una Instancea (snapshot) de los datos y archivos para cargarlos al git
git add .

// Confirma la Instancia (snapshot):
git commit -m "Se agrego un ListTile con Dismissble en el Home - Fin Seccion 1"

// Muestra todas las veriones en el git local y remote:
git tag

// Guarda una version en el git local y remote:
git tag -a v0.0.2 -m "Se agrego un ListTile con Dismissble en el Home - Fin Seccion 2"

// subir los tags al remote (remote):
git push --tags


// GITHUB:
// Guarda una version en el remote:
git remote add origin git@github.com:sanderhernandez/socket.io-backend-nodejs-express.gi
git remote add origin git@github.com:sanderhernandez/socket.io-server-nodejs-express.git
git branch -M main
git push -u origin main

// Ver Remote configurado en la PC:
git remote -v

// Reconfigurar la url del remote en la PC:
git remote set-url origin <remote-url>

// Restablece todos los archivos subidos, despues del commit:
git checkout -- .

// Eliminar la configuracion de git en la PC:
rm -fr .git

******************************************************************************************

/// BACKEND

// Instalando nodejs y npm:
apt-get install node nmp

// Iniciar un Proyecto de node y npm:
npm init -y

// Instalar librerias:
npm i express
sudo npm i -g nodemon
npm i dotenv
npm i socket.io
npm i uuid@8.3.0




// Ejecutar la app con node:
node index.js
ó
npm start

// Ejecutar la app con nodemon:
nodemon index.js
ó
npm run start:dev

******************************************************************************************
